interface ChatCommunityProps{
    viewerName:string,
    hostName:string,
    isHidden:boolean
}

export const ChatCommunity=({
    viewerName,
    hostName,
    isHidden
}:ChatCommunityProps)=>{
   return (
    <div></div>
   )
}